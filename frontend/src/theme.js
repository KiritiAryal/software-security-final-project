import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0052cc",
    },
    secondary: {
      main: "#edf2ff",
    },
    navbarcolor: {
      main: "#ffff",
    },
    buttoncolor: {
      main: "#000000",
    },
  },
});

export default theme;
